const sum = (total, item) => {
    total + item;
}

const calculator = (() => {
    if 
    const add = (...args) => {
        let result = args.reduce(function(total, item) {
            return total + item;
        },0);
        return result;
    }

    const subtract = (a, b) => {
        
    }



    return {add};

})();


module.exports = calculator;
